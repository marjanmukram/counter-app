import { makeStyles } from "@material-ui/styles";

export const zero = makeStyles(() => ({
  zButton: {
    color: "white",
    padding: "20px",
    border: "3px solid black",
    backgroundColor: "liteblue",
    marginLeft: "200px"
  }
}));

export const plusMinusDelete = makeStyles(() => ({
  pmdButton: {
    color: "white",
    padding: "20px",
    border: "3px solid black",
    backgroundColor: "litepink",
    marginLeft: "20px"
  }
}));
